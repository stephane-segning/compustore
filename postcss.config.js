/**
 * PostCSS configuration file
 * @type {import('postcss-load-config').Config}
 */
const config = {
  plugins: {
    cssnano: {},
    autoprefixer: {},
    tailwindcss: {},
  },
};

export default config;
